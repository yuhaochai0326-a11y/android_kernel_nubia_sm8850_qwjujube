%YAML 1.2
---
$id: http://devicetree.org/schemas/hfi-core.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#

title: HFI Core

description: |
  HFI Core implements Linux APIs to initialize, deinitialize, register-for-signal, and
  overall manage the hfi communication with DCP.

properties:
  compatible:
    const: qcom,msm-hfi-core

examples:
  - |
    msm_hfi_core: qcom,hfi-core {
       compatible = "qcom,msm-hfi-core";
       status = "ok";
    };
...
